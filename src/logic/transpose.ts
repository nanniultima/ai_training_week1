import type {
  TransposedMusic,
  TranspositionRequest,
} from "../types.js";

/** Transponoi putkimerkeillä tahdistetut musiikkirivit. */
export function transposeMusic(
  _request: TranspositionRequest,
): TransposedMusic {
  throw new Error("not implemented");
}
